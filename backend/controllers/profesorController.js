//Importar librerías
const mongoose = require('mongoose');
mongoose.pluralize(null);

const Estudiantes = require('../models/profesor');

const controller = {
    obtenTodos: async (req,res) => {
        
    }
}

module.exports = controller;